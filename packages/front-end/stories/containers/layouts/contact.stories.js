import React from "react";
import Contact from "../../../components/containers/layouts/contact";

export default {
    title: 'containers/layouts/contact',
    component: Contact,
}

const Template = (args) => <Contact {...args}/>
export const contact_us = Template.bind({});